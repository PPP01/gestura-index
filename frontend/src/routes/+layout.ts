// adapter-static: alle öffentlichen Seiten werden beim Build prerendered.
// Der Admin-Bereich setzt das später in seinem eigenen Layout auf false
// (client-only SPA mit fallback-Seite).
export const prerender = true;
